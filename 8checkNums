//Function that takes in two numbers, num1 and num2, and returns either true or false or '-1'.
//If num2 > than num 1, then true is returned, otherwise false is returned.
//If the two numbers are equal then the string '-1' is returned.

function CheckNums(num1,num2) { 
	if (num2 > num1) {
	   return true;
   	}
  	else if (num2 < num1) {
      	  return false;
    	}	
  	else if (num2 == num1) {
     	  return '-1';
    	};        
}
